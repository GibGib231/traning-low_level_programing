#include<stdio.h>
#include"main.h"
void _puts(char *str)
{
	puts(str);
	return(0);
}
int main()
{
//	printf("Amb codeing with is soso cool");
	_puts("i do not fear computer, but i fear the leack of them");
	return(0);
}
